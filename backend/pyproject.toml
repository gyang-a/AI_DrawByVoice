[project]
name = "voice-canvas-backend"
version = "0.1.0"
description = "FastAPI backend scaffold for VoiceCanvas."
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.3,<1.0.0",
    "langchain>=1.3.8",
    "langchain-ollama>=1.1.0",
    "pydantic>=2.13.4,<3.0.0",
    "websocket-client>=1.9.0,<2.0.0",
]

[dependency-groups]
dev = []

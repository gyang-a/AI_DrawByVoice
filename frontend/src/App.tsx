import './App.css';

function App() {
  return (
    <main className="app-shell" aria-labelledby="app-title">
      <section className="app-card">
        <p className="app-eyebrow">VoiceCanvas</p>
        <h1 id="app-title">AI 语音绘图工具</h1>
        <p>
          项目脚手架已初始化。后续 PR 将逐步加入页面布局、绘图命令协议、画布渲染、AI 指令解析和语音能力。
        </p>
      </section>
    </main>
  );
}

export default App;
